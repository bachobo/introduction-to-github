import smtplib
from email.mime.text import MIMEText

send_email = 'sushi__o@naver.com'
send_pwd = 'q1w2e3r4t5.?'

recv_email = 'ohsungmin1206@gmail.com'

smtp_name = 'smtp.naver.com'
smtp_port = 587

text = """
메일 내용을 여기에 적어요
여러줄을 입력해도 됩니다.
"""

msg = MIMEText(text)

msg['Subject'] = "메일 제목이다_파이썬"

msg['From'] = send_email
msg['To'] = recv_email

print(msg.as_string())

s=smtplib.SMTP(smtp_name, smtp_port)
s.starttls()

s.login(send_email, send_pwd)

s.sendmail(send_email, recv_email, msg.as_string())
s.quit()
